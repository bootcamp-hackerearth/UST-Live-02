// rename this to environemnt when running locally
export const environmentDevelopment = {
  production: false,
  apiUrl: 'http://localhost:5000',
  debugMode: true,
  pageSize: 5,
};

export const environment = {
  production: true,
  apiUrl: '',
  pageSize: 5,
};
